# greetd's greeter gets a logind session, but XDG_RUNTIME_DIR does not reach
# the greeter process, and Hyprland aborts outright without one. Provide a
# runtime dir of its own rather than colliding with logind's /run/user tree.
d /run/greetd 0700 greetd greetd -
